import java.io.*;import java.util.*;import agiStruct.*;import AGIUtil.*;import coreMethods.*; import topLevelMethods.*; public class { public () throws Exception {  String path = "/home/agi/Desktop/eclipse/AGI/bin/";for (int i = 0; i < currentErrorLocations.size(); i++) {File contextSource= new File("/home/agi/Desktop/eclipse/AGI/bin/contextSource.txt"); FileWriter errorWriter = new FileWriter(contextSource, true); errorWriter.write(currentErrorLocations.get(i).getParentClass() + ", " + currentErrorLocations.get(i).getInstructionNumber() + ", " + currentErrorLocations.get(i).getInstruction());}} }