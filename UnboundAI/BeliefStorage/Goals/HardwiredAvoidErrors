Goal:100,,,
if (currentErrorLocations.size() == 0){
goalEvalResult.setComplete(true);
} else {
goalEvalResult.setComplete(false);
}
int resultOutput = 0;
int obvError
if (goalEvalResult.complete){
resultOutput = 20;
} else if (currentErrorLocations.get(0).instructionNumber != prevErrorLocations.get(0).instructionNumber){
resultOutput = 200;
} else {
resultOutput = -100;
}
System.out.println("ShouldNotThrowErrors result: " + resultOutput);
goalEvalResult.setResult(resultOutput);

